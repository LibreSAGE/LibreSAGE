#pragma once

// D3D9 compatibility shim -- texture subset. The texture/surface helpers this
// project uses are declared in d3dx9core.h (mirroring the original D3DX8 shim
// layout, where d3dx8tex.h simply forwarded to d3dx8core.h).
#include <d3dx9core.h>
